# macOS
.DS_Store
._*
.Spotlight-V100
.Trashes

# Editors
.vscode/
.idea/
*.swp

# Node (only if a toolchain is ever added — the site itself needs no build)
node_modules/
npm-debug.log*
.env
.env.local

# Local scratch
*.log
tmp/
